# Wind farm area polygon, viewshed, and impact {#sec-e06}

> **Dates of practical:** 20th and 27th November and 11th December 2025

This exercise is all about applying the skills you've gained over the past five exercises, to cement your GIS skills and successfully complete the wind farm project. You will also create a new polygon to represent your wind farm location, and create viewsheds to identify who may be impacted by the wind farm development. 

**This exercise will guide you through the requirements for making Map 4**

::: {.callout-note}
## Learning aims

Create and edit a polygon to represent a wind farm, calculate its area, create a viewshed, and count the population living within view of the proposed wind farm.
:::

## Starting point

(@) Launch QGIS and create a new project

(@) Add the various constraint vector and wind speed raster layers within your map tile (created from previous chapters) to the project

## Wind farm polygon creation

#### Create new vector layer

(@) Create a new GeoPackage vector layer representing your optimal wind farm location:

    i. Click `Layer` → `Create Layer` → `New GeoPackage Layer...`
    ii. Click the `Browse` icon (![](figs/Browse.png){width=3%}) next to `Dataset` to save the wind farm location vector somewhere sensible
    iii. Set `Geometry type` as `Polygon`
    iv. Set the CRS as `EPSG:27700`
    v. Click `OK`
    vi. From the `Layers` panel, right‑click on the new layer and click `Toggle Editing` or find the shortcut (![](figs/ToggleEditing.png){width=3%})
    
    You can tell when a layer's in edit mode, as a pencil icon appears over the layer's symbol. Edits are temporary until you save them. This allows you to undo any mistakes you may have made. But beware - once saved, mistakes can't be undone!  

    vii. From the `Digitizing Toolbar`, click `Add Polygon Feature`
    
    ![](figs/DigitizingToolbar.png){fig-align='center' width=50%}
    
    viii. Left‑click on the Map Canvas to draw vertices (points), outlining your optimum wind farm location
    ix. Once your polygon is complete, right‑click to finish
    x. click `OK` 
    xi. If necessary, adjust the polygon by:
        a. Clicking the `Vertex Tool` (![](figs/VertexTool.png){width=3%}) 
        b. Selecting a polygon vertex 
        c. Moving the cursor to the desired location and pressing left-click
    xii. Click the `Toggle Editing` icon (![](figs/ToggleEditing.png){width=3%}) once more to turn off editing
    xiii. Click `Save`

::: {.callout-hint}
## Enable snapping

Are you a perfectionist, but you can't get the edge of your optimum wind farm area to abut a constraint layer? Well the snapping tool is for you! This will pull the cursor up against the edge of a vector layer to aid in the perfect placement of vertices. Try it out by clicking `View` → `Toolbars` → `Snapping Toolbar`, then clicking the `Enable Snapping` icon (![](figs/EnableSnapping.png){width=3%}).
:::

## How big is your wind farm?

(@) Calculate the area of the polygon (see @sec-fieldcalc)

A 50 m turbine generates about 750kWh . How many turbines will your windfarm support? Note that as long as your area is at least 150m across at its narrowest point it is big enough for at least one turbine, so is viable.

(@) To create:

    i. Click `Vector` → `Research Tools` → `Create Grid...`
    ii. Set `Grid type` as `Hexagon (Polygon)`
    iii. Under `Grid extent`, click the drop-down arrow → `Calculate from Layer` → select your wind farm location polygon
    iv. Set `Horizontal spacing` and `Vertical spacing` to `50` metres
    v. Set `Grid CRS` as `EPSG:27700`
    vi. Click `OK` → `Close`
    vii. Click `Vector` → `Geometry Tools` → `Centroids...`
    viii. Set `Input layer` to the new `Grid` layer
    ix. Click `Run` → `Close`
    x. Create buffer around those points
    x. Use `Select by Location...` BUT this time, only `are within` for your wind farm site(see)
    xi. Export, check 'save selected features'
    xii. Use attribute table to count number of turbines
    xiii. Calculate their potential energy output in kWh
    
    
    

## Creating a viewshed

(@) Install the **Visibility Analysis** plugin.

(@) Create a centroid point for your wind farm polygon (**Vector → Geometry Tools → Centroids**). This point represents the wind farm location for viewshed analysis.

(@) In the **Processing Toolbox**, run **Visibility Analysis → Create Viewpoints**. Set Observer locations = wind farm centroid; DEM = your tile DEM; Radius of analysis = 10 km; Observer height = 50 m; Target height = 2 m. Save the output viewpoint layer.

(@) Run **Visibility Analysis → Analysis → Viewshed** using the viewpoint layer and your tile DEM. Explore and understand the result.

## Calculating impact

(@) Vectorise your viewshed (see Exercise 05) to create polygons representing areas within 10 km from which the wind farm is visible. Use select‑by‑value and save selected features.

(@) Using methods from Exercise 03, calculate the total number of people registered at postcodes within line‑of‑sight. If errors about “invalid geometry” occur, buffer the visibility polygon by 1 m and retry.

(@) Design **Map 4** to present the wind farm location/extent and its visual impact. Focus on population and visibility; you do not need to repeat earlier context maps.

(@) Comment in your CW4 caption on how well your analysis captures potential impact if your wind farm lies near the tile edge.

::: {.callout-tip}
# Learning outcomes

By the end of this exercise you should:

1. Create and edit a vector layer in QGIS.
2. Create a new polygon layer to represent a proposed wind farm location.
3. Calculate the area of a vector polygon and estimate turbine capacity.
4. Create a viewshed using an observer/target and a DEM.
5. Quantify population within sight of the proposed wind farm and its proportion of the tile population.
:::
